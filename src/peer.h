#include <bits/stdc++.h>

using namespace std;

class Peer{
	
public:
    string id;
    string ip;
    int port;
    float bandWidth;
    float disconnectRate;

    float spacePrice;
    float onlineTime;

    Peer(string peerId);
    
};
